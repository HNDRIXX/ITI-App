import { View, Text, StyleSheet, FlatList } from "react-native";

export default function AnnouncementSection () {
    return (
        <View>
            <Text>Announcement Section</Text>
        </View>
    )
}

const style = StyleSheet.create({
    container: {
        flex: 1,
    }
})